{
  "name": "jeremyjanin.com",
  "version": "2.0.0",
  "description": "https://jeremyjanin.com",
  "scripts": {
    "build": "hugo -b https://${BASE_URL:-bonjour-git-$VERCEL_GIT_COMMIT_REF-ys1.vercel.app}",
    "build-preview": "hugo -F -D -b https://${BASE_URL:-bonjour-git-$VERCEL_GIT_COMMIT_REF-ys1.vercel.app}",
    "debug": "rm -rf public/* resources/_gen && hugo --templateMetrics --templateMetricsHints",
    "dev": "hugo serve --disableFastRender -p $PORT",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yannick Schutz",
  "license": "MIT",
  "dependencies": {
    "@fullhuman/postcss-purgecss": "^4.0.0",
    "@tailwindcss/aspect-ratio": "^0.2.0",
    "@tailwindcss/line-clamp": "^0.2.0",
    "@tailwindcss/typography": "^0.3.1",
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.10",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.2.19"
  }
}
